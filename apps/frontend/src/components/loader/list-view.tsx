import { ListViewProps } from '@type/components/list-view'

export default function ListView({ canCreate = true }: ListViewProps) {
  return <section className='flex h-[calc(100dvh-120px)]'></section>
}
